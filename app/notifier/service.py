class Notifier:
    
    async def send_message(self, message: MessageTemplate):
        '''
        should send income message. smth like
        Bot.send_message(**message)
        where message: MessageTemplate and contains key value pairs
        text=message_text: str
        reply_markup=markup: Optional[InlineMarkup/other markup]
        and other optional fields
        '''
        
        pass
    
    pass
